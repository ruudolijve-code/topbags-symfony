    {# --- LAAG 3: SERVICE BAR (Gecentreerd + Geen verticale lijn) --- #}
    <div class="bg-white border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-6 h-10 flex items-center justify-center">
            
            <ul class="flex items-center gap-8 h-full">

                <li>
                    <a href="{{ path('brand_index') }}" class="text-[11px] font-bold text-gray-500 hover:text-amber-600 transition-colors uppercase tracking-widest">
                        Merken
                    </a>
                </li>

                <li>
                    <a href="{{ path('travelmiles_index') }}" class="text-[11px] font-bold text-gray-500 hover:text-amber-600 transition-colors uppercase tracking-widest">
                        
                        Travelmiles Sparen
                    </a>
                </li>

                                <li>
                    <a href="{{ path('travelmiles_index') }}" class="text-[11px] font-bold text-gray-500 hover:text-amber-600 transition-colors uppercase tracking-widest">
                        
                        Inspiratie & Advies
                    </a>
                </li>

            </ul>

        </div>
    </div>